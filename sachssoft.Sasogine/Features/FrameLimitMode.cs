namespace sachssoft.Sasogine.Features;

public enum FrameLimitMode
{
    Default,       // Standard (z. B. 60 FPS / VSync)
    Unlimited,     // Kein Limit, maximal mögliche FPS
    Custom         // Benutzerdefinierte Begrenzung
}